apiVersion: ingress.v1.haproxy.org/v1
kind: Backend
metadata:
  name: unifi
  namespace: external-services
spec:
  config:
    name: unifi
    mode: http
    balance:
      algorithm: "leastconn"
    abortonclose: disabled
    forwardfor:
      enabled: enabled
    default_server:
      ssl: enabled
      check: enabled
      verify: none
---
apiVersion: v1
kind: Service
metadata:
  name: unifi
  namespace: external-services
spec:
  clusterIP: None
  ports:
  - protocol: TCP
    name: https
    port: 443
---
apiVersion: v1
kind: Endpoints
metadata:
  name: unifi
  namespace: external-services
subsets:
- addresses:
  - ip: 192.168.10.6
  ports:
  - port: 8443
    name: https
---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: unifi
  namespace: external-services
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    cr-backend: external-services/unifi
spec:
  ingressClassName: haproxy
  rules:
  - host: unifi.boo.eth0.work
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: unifi
            port:
              name: https
  tls:
  - secretName: unifi-tls
    hosts:
    - unifi.boo.eth0.work
